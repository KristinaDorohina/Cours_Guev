from collections import Counter

witcher_inventory = Counter([i.lower() for i in input().split()])

for i in witcher_inventory:
    printmin(witcher_inventory.values())):
    print(witcher_inventory[i])